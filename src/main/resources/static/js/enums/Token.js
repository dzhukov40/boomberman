/**
 * Модуль для локальных запросов к беку на нашем домене
 * @module HttpLocal
 */
    // TODO: засунуть его в сервис, наверно
export const TOKEN = {
        JWT: 'JWT_TOKEN',
        AUTH: 'Authorization'
    };
