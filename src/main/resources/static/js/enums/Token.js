/**
 * Модуль для локальных запросов к беку на нашем домене
 * @module HttpLocal
 */
(function () {

    const TOKEN = {
        JWT: 'JWT_TOKEN'

    };


    window.TOKEN = TOKEN; // экспортируем класс

})();
